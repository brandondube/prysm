language: python
python:
  - "3.6"
install:
  - pip install .
  - pip install -r requirements.txt
  - pip install -q pytest pytest-cov coveralls
cache: pip
script:
  - pytest
notifications:
  email: false
after_success:
  - coveralls
